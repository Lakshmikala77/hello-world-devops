name: testing self hosted agent runner

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Hello
        run: echo "Running on self-hosted runner"
